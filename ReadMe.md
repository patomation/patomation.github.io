Getting Started:

1 install ruby
2 install jekyll and bundler with gem
```
gem install jekyll or something
gem install bundler
```

Local Serve:
```
bundle exec jekyll serve
```

Set Browser to:
```
http://localhost:4000/
```
